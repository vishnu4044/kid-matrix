VITE_API_BASE_URL=http://127.0.0.1:5000/api
# To test from an iPad/phone on the same WiFi, use your machine's LAN IP
# instead, e.g. VITE_API_BASE_URL=http://192.168.1.42:5000/api, and add that
# same origin (port 5173) to the backend's CORS_ORIGINS.
